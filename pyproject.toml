[project]
name = "yzh-blog"
version = "0.1.0"
description = "YZH的Blog — Markdown 即文章的纯静态技术博客"
requires-python = ">=3.11"
dependencies = [
    "markdown>=3.6",
    "pygments>=2.18",
    "jinja2>=3.1",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
