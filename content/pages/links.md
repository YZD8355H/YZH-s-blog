---
title: 友链
---

# 友链

这里的友链条目统一维护在 `config.yaml` 的 `links` 字段中：

```yaml
links:
  - name: "站点名"
    url: "https://example.com"
    desc: "一句话介绍"
```

修改后重新运行 `uv run build.py` 即可生效。当前收录：

<!-- links 由 config.yaml 自动渲染，以下为构建时的实际内容占位 -->
